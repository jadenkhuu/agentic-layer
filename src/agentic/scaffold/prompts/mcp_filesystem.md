# Repo overview via filesystem MCP

You have access to the **filesystem** MCP server. Use it to enumerate the
top-level entries of the target repo and write a concise overview.

Task hint: {{TASK}}

Write the overview to `{{OUTPUT}}`. Cover:

1. What language / framework the repo uses (1 sentence).
2. The top-level directories and what each one is for (one line each).
3. The single entry point you'd start reading from to understand the
   project (file path + one-line reason).

Keep it under 30 lines.
